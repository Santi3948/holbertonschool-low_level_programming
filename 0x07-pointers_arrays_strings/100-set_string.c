#include "main.h"
#include <stdio.h>
/**
 * set_string - finds the first occurrence of the substring
 * @s: string to be evaluated
 * @to: string to be evaluated
 * Return: pointer to dest.
 */

void set_string(char **s, char *to)
{
*s = to;
}
